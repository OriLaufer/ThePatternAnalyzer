<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0B1121"
    tools:context=".MainActivity">
    <!-- 1. ה-HEADER (החלק העליון המעוצב) -->

    <!-- זהו "מיכל פנימי" שמחזיק את הלוגו והכותרות ביחד בחלק העליון -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/top_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="20dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:background="#0B1121"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <View
            android:id="@+id/logo_background"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/header_backround"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- ב. האייקון הלבן (נרות יפניים) -->
        <!-- יושב *מעל* הרקע הסגול וממורכז בתוכו -->
        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/header_logo"
            app:tint="#FFFFFF"
            app:layout_constraintStart_toStartOf="@id/logo_background"
            app:layout_constraintEnd_toEndOf="@id/logo_background"
            app:layout_constraintTop_toTopOf="@id/logo_background"
            app:layout_constraintBottom_toBottomOf="@id/logo_background" />


        <TextView
            android:id="@+id/header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="Pattern Analyzer"
            android:textColor="#A78BFA"
            android:textSize="20sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-black"
            app:layout_constraintStart_toEndOf="@id/logo_background"
            app:layout_constraintTop_toTopOf="@id/logo_background"
            app:layout_constraintBottom_toTopOf="@id/header_subtitle"
            app:layout_constraintVertical_chainStyle="packed" />

        <!-- ד. כותרת משנה (מתחת לשם האפליקציה) -->
        <TextView
            android:id="@+id/header_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI TRADING ASSISTANT"
            android:textColor="#64748B"
            android:textSize="11sp"
            android:letterSpacing="0.05"
            android:fontFamily="sans-serif-medium"
            app:layout_constraintStart_toStartOf="@id/header_title"
            app:layout_constraintTop_toBottomOf="@id/header_title"
            app:layout_constraintBottom_toBottomOf="@id/logo_background" />

        <!-- כפתור יציאה / החלפת משתמש -->
        <ImageView
            android:id="@+id/btn_logout"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/outline_arrow_right_alt_24"
            android:clickable="true"
            android:focusable="true"
            android:foreground="?attr/selectableItemBackgroundBorderless"
            app:tint="#64FFDA"
            android:contentDescription="Logout"
            tools:ignore="TouchTargetSizeCheck"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>


    <!-- ============================== -->
    <!-- 2. המיכל לתוכן המתחלף -->
    <!-- ============================== -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        tools:ignore="FragmentTagUsage"
        app:layout_constraintTop_toBottomOf="@id/top_header"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- ============================== -->
    <!-- 3. התפריט התחתון -->
    <!-- =============עיצוב של גוגל לסרגל תפריט תחתון=============== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#1E293B"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:menu="@menu/bottom_nav_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>